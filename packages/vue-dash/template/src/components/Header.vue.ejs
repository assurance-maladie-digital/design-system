<template>
	<VLayout
		class="header pb-3 primary"
		justify-center
		column
		align-center
		tag="header"
		role="header"
	>
		<RouterLink
			<% if (i18n) { %>:aria-label="$t('components.header.logoBtn.label')"<% } else { %>aria-label="Accueil"<% } %>
			:to="{ name: 'home' }"
			exact
			class="logo-link white"
		>
			<img
				<% if (i18n) { %>:alt="$t('components.header.logoBtn.alt')"<% } else { %>alt="l'Assurance Maladie"<% } %>
				src="../assets/logo.svg"
				class="d-block mx-auto logo"
			>
		</RouterLink>

		<VLayout class="inner-toolbar">
			<h1 class="white--text headline mt-4">
				<% if (i18n) { %>{{ $t('components.header.title') }}<% } else { %>Projet demo<% } %>
			</h1>
		</VLayout>
	</VLayout>
</template>

<script lang="ts">
	import Vue from 'vue';
	import Component from 'vue-class-component';

	/** The Header of the application */
	@Component
	export default class Header extends Vue {}
</script>

<style lang="scss" scoped>
	.header {
		flex: none;
		position: relative;

		&::after {
			top: 0;
			left: 0;
			z-index: 0;
			width: 100%;
			content: "";
			height: 45px;
			position: absolute;
			background: #fff;
		}
	}

	.logo-link {
		z-index: 1;
		overflow: hidden;
		min-width: 155px;
		position: relative;
		display: inline-block;
		border-bottom-left-radius: 9px;
		border-bottom-right-radius: 9px;
	}

	.logo {
		height: 75px;
		padding: 8px;
	}

	.inner-toolbar {
		width: 100%;
		position: relative;
		justify-content: center;

		h1 {
			width: 100%;
			text-align: center;
		}
	}

	@media only screen and (max-width: 750px) {
		.inner-toolbar h1 {
			max-width: 280px;
		}
	}

	@media only screen and (max-width: 600px) {
		.inner-toolbar {
			align-items: center;
			flex-direction: column;
		}
	}
</style>
