import dayjs from 'dayjs';

export default function formatDate(date: string | undefined): string {
	// If no date is provided, return empty string
	// else, format
	return date === undefined ? '' : dayjs(date).format('DD/MM/YYYY');
}
