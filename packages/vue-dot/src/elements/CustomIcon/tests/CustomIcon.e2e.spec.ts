import { mountComponent } from '@/tests/e2e';
import CustomIcon from '../';

describe('CustomIcon', () => {
	// const textToCopy = 'Texte copié !';

	beforeEach(() => {
		mountComponent(CustomIcon);
	});
});
