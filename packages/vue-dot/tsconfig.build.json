{
	// Extend root config
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"baseUrl": ".",
		"types": [
			"node",
			"jest"
		],
		"paths": {
			// Specific alias used in tests
			"vd-src/*": [
				"src/*"
			]
		}
	},
	"include": [
		// Playground
		"playground/**/*.ts",
		"playground/**/*.vue",
		// Src
		"src/**/*.ts",
		"src/**/*.vue",
		// JSON
		"src/**/*.json"
	],
	"exclude": [
		"node_modules"
	]
}
